@model blog_system.Models.BlogPost
@*

    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{

}


 @*When we use '@' - it's to go form

 ViewData is weakly typed and can store any time of data, so an object of the base class 'object' in  returned. The runtime does not know the specific type of object stored

 so we have to declare the type
    (this is old code that has beenr removed)


     *@

<h2>@Model.Title</h2>

